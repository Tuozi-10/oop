using UnityEngine;

public enum ObjectType
{
    Wood = 1,
    Rock = 2,
    Sword = 4
}

public enum EntityType
{
    Wanderer = 1,
    Harvester = 2,
    Miner = 4
}

public enum CurrentTarget
{
    Base,
    Harvest
}
